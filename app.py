# This file is no longer used.
# The application has been migrated from Streamlit to FastAPI.
#
# To start the API server, run:
#   python server.py
#
# Or with uvicorn directly:
#   uvicorn server:app --reload --host 0.0.0.0 --port 8000
